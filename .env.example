# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=streamystats

# Streamystats API Key for external API access
# This is required for API routes that use API key authentication
# Generate a secure random string for this value
STREAMYSTATS_API_KEY=your-secure-api-key-here

# Optional: Version and build information (set automatically in CI/CD)
VERSION=latest
COMMIT_SHA=unknown
BUILD_TIME=unknown

# Application Configuration
NODE_ENV=production
PORT=3000